% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_bec_description.R
\name{add_bec_description}
\alias{add_bec_description}
\title{Add BEC descriptions from Comex Stat}
\usage{
add_bec_description(
  x,
  lang = c("en", "pt", "es"),
  level = c("n3", "n2", "n1", "all"),
  drop_code = TRUE
)
}
\arguments{
\item{x}{A data frame containing an \code{ncm} column.}

\item{lang}{Language of the appended BEC descriptions. Must be one of
\code{"en"}, \code{"pt"}, or \code{"es"}.}

\item{level}{BEC aggregation level to return. Must be one of \code{"n3"}, \code{"n2"},
\code{"n1"}, or \code{"all"}.}

\item{drop_code}{Logical. If \code{TRUE} (default), removes joined BEC code
columns from the result.}
}
\value{
A data frame with the same rows as \code{x}, plus the selected BEC
description columns for the requested language. When \code{drop_code = FALSE},
the corresponding BEC code columns are also retained.
}
\description{
Uses the NCM and BEC correlation tables to map each \code{ncm} in \code{x} to its
Broad Economic Category (BEC) classification and append description columns
at the requested aggregation level.
}
\details{
The function first joins \code{x} to the NCM table to recover \code{bec_n3_code} and
then joins the BEC table to retrieve the requested descriptions. When
\code{level = "all"}, descriptions for all available BEC levels are returned.
When \code{level} is \code{"n2"} or \code{"n1"}, intermediate \code{n3} columns are removed from
the final output.
}
\examples{
\dontrun{
library(dplyr)
df <- tibble(ncm = c("01012100", "02011000"))

# Add BEC descriptions at level n3 in Portuguese
add_bec_description(df, lang = "pt", level = "n3")

# Add all BEC levels in English
add_bec_description(df, lang = "en", level = "all")
}

}
