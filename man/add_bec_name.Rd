% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_bec_name.R
\name{add_bec_name}
\alias{add_bec_name}
\title{Add BEC names from Comex Stat}
\usage{
add_bec_name(
  x,
  lang = c("en", "pt", "es"),
  level = c("n3", "n2", "n1", "all"),
  drop_code = TRUE
)
}
\arguments{
\item{x}{A data frame containing an \code{ncm} column.}

\item{lang}{Language of the appended BEC names. Must be one of \code{"en"},
\code{"pt"}, or \code{"es"}.}

\item{level}{BEC aggregation level to return. Must be one of \code{"n3"}, \code{"n2"},
\code{"n1"}, or \code{"all"}.}

\item{drop_code}{Logical. If \code{TRUE} (default), removes joined BEC code
columns from the result.}
}
\value{
A data frame with the same rows as \code{x}, plus the selected BEC name
columns for the requested language relocated immediately after \code{ncm}. When
\code{drop_code = FALSE}, the corresponding BEC code columns are also retained
in the same block.
}
\description{
Uses the NCM and BEC correlation tables to map each \code{ncm} in \code{x} to its
Broad Economic Category (BEC) classification and append name columns at the
requested aggregation level.
}
\details{
The function first joins \code{x} to the NCM table to recover \code{bec_n3_code} and
then joins the BEC table to retrieve the requested names. When
\code{level = "all"}, names for all available BEC levels are returned. When
\code{level} is \code{"n2"} or \code{"n1"}, intermediate \code{n3} columns are removed from the
final output. The joined BEC columns are always placed immediately after
\code{ncm}.
}
\examples{
\dontrun{
library(dplyr)
df <- tibble(ncm = c("01012100", "02011000"))

# Add BEC names at level n3 in Portuguese
add_bec_name(df, lang = "pt", level = "n3")

# Add all BEC levels in English
add_bec_name(df, lang = "en", level = "all")
}

}
