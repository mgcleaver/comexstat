% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_cuci_name.R
\name{add_cuci_name}
\alias{add_cuci_name}
\title{Add CUCI names from Comex Stat}
\usage{
add_cuci_name(
  x,
  level = c("basic_heading", "subgroup", "group", "division", "section", "all"),
  drop_code = TRUE
)
}
\arguments{
\item{x}{A data frame containing an \code{ncm} column.}

\item{level}{CUCI aggregation level to return. Must be one of
\code{"basic_heading"}, \code{"subgroup"}, \code{"group"}, \code{"division"}, \code{"section"},
or \code{"all"}.}

\item{drop_code}{Logical. If \code{TRUE} (default), removes joined CUCI code
columns from the result.}
}
\value{
A data frame with the same rows as \code{x}, plus the selected CUCI name
columns. When \code{drop_code = FALSE}, the corresponding CUCI code columns are
also retained.
}
\description{
Uses the NCM and CUCI correlation tables to map each \code{ncm} in \code{x} to its
CUCI classification and append Portuguese name columns for the requested
aggregation level.
}
\details{
Only Portuguese CUCI names are available in the source table. The function
first joins \code{x} to the NCM table to recover \code{cuci_basic_heading_code}, then
joins the CUCI table. When \code{level = "all"}, all CUCI name levels are
returned. When \code{level} is not \code{"basic_heading"} or \code{"all"}, intermediate
\code{basic_heading} columns are removed from the final output.
}
\examples{
\dontrun{
library(dplyr)

# Example input tibble with NCM codes
df <- tibble(ncm = c("01012100", "02011000"))

# Add CUCI names at the group level
df_with_cuci <- add_cuci_name(df, level = "group")

# Add CUCI names at all levels
df_all <- add_cuci_name(df, level = "all")
}

}
